# 法鬥跑跑 隱私權政策

最後更新：2026 年 8 月 16 日

法鬥跑跑（Frenchie）是一個 Chrome 擴充功能，唯一用途是：**在你瀏覽的網頁上放一隻可以照顧和互動的虛擬法國鬥牛犬。**

**我們沒有伺服器。** 這個擴充功能沒有任何後端服務，開發者收不到、也看不到你的任何資料。

---

## 一、我們儲存什麼

以下資料全部存在你自己的瀏覽器裡，使用 Chrome 提供的 `chrome.storage` API：

| 資料 | 內容 | 存在哪裡 | 會不會同步 |
| --- | --- | --- | --- |
| 寵物資料 | 你取的名字、等級、經驗值、飽食度、體力、親密度、清潔度、餵食／玩耍／撫摸次數 | `chrome.storage.local`（本機） | 否 |
| 外觀與行為設定 | 身高、速度、是否留腳印、是否自動走動、哪些網域關閉 | `chrome.storage.sync` | 是，會透過你的 Google 帳號同步到你自己的其他裝置 |
| AI 設定 | 你選的供應商、模型名稱、Base URL、**API 金鑰** | `chrome.storage.local`（本機） | **否，金鑰不會同步** |
| 聊天記錄 | 你和寵物的最近 200 則對話 | `chrome.storage.local`（本機） | 否 |

移除擴充功能時，上述資料會一併被 Chrome 刪除。

## 二、我們不蒐集什麼

- 不蒐集瀏覽紀錄
- 不蒐集你造訪的網址清單
- 不蒐集網頁內容、表單輸入、帳號密碼
- 不蒐集個人識別資訊、位置、聯絡資料
- 沒有分析工具、沒有廣告、沒有第三方 SDK
- 不販售、不轉移任何資料給第三方

## 三、AI 對話功能（選用，預設關閉）

這個功能預設是關閉的。只有在你自己到設定裡開啟、並填入**你自己的** API 金鑰之後才會運作。

啟用後：

- 你的瀏覽器會**直接**向你指定的 API 端點（Anthropic、Google Gemini，或任何你填入的 OpenAI 相容端點）發送請求。請求不經過我們，開發者不會收到任何內容。
- 送出的內容包含：你輸入的訊息、最近的對話記錄、以及寵物當下的數值（例如「有點餓、精神很好」）。
- **API 金鑰只存在你的電腦上**，不會同步、不會上傳。
- 使用這些第三方 API 會受該供應商的隱私權政策與收費規範約束，費用由你自行支付。

### 頁面標題（額外選用，預設關閉）

設定中有一項「讓牠知道頁面標題」，**預設關閉**。開啟後，AI 請求會額外附上目前分頁的 `document.title` 的前 80 個字元，讓寵物可以對你正在看的東西有反應。

- 只送標題，**不送網址、不送內文、不送留言、不送你輸入的任何內容**。
- 這些內容一樣只送到你自己指定的 API 端點，不經過我們。
- 隨時可以關閉。

## 四、權限用途

| 權限 | 為什麼需要 |
| --- | --- |
| `storage` | 儲存寵物資料與設定 |
| `scripting` | 在你授權的網站上放置寵物 |
| `activeTab` / `tabs` | 讀取目前分頁的網域，用來顯示「要不要在這個網站放寵物」的開關 |
| `sidePanel` | 開啟側邊欄（狀態、聊天、設定） |
| 指定網站的存取權 | 在該網站的網頁上繪製寵物 |
| 所有網站的存取權（選用） | 只有在你主動按下「讓牠去所有網站」時才會要求。信箱、雲端硬碟、登入頁與金流頁一律排除 |

## 五、兒童

本擴充功能不針對 13 歲以下兒童設計，也不會刻意蒐集兒童資料。

## 六、政策變更

若資料處理方式有變更，我們會更新本頁面並在擴充功能內主動通知你。

## 七、聯絡方式

有任何問題請寄信到：<在這裡填入你的聯絡信箱>
